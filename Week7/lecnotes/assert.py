def square(x):
    return x + x


assert square(10) == 100
# makes a statement, verified if its correct -> basically making sure things are going right
# instead of just finding out some weird error is happening on the final result
